import { AuthorizathionGuard } from './authorizathion.guard';

describe('AuthorizathionGuard', () => {
  it('should be defined', () => {
    expect(new AuthorizathionGuard()).toBeDefined();
  });
});
